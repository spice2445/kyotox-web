import Image from "next/image"
import { styled } from "../../../lib/styles"

export const WalletIcon = styled("div", {
  display: "flex",
  alignItems: "center",
})

export const ImageWallet = styled(Image, {
  marginRight: "5px",
})
