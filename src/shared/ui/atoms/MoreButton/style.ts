import { styled } from "../../../lib/styles"

export const WrapperButton = styled("button", {
  color: "$red",
  fontSize: "$small",
  fontWeight: "$medium",
  lineHeight: "24px",
  background: "inherit",
})
