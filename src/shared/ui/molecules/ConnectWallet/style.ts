import { styled } from "../../../lib/styles"

export const ConnectWalletWrapper = styled("div", {
  display: "flex",
  alignItems: "center",
  cursor: "pointer",
})

export const WrapperConnectButton = styled("div", {
  marginLeft: "22px",
})
